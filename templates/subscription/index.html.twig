{% extends 'base.html.twig' %}

{% block title %}Subscribe to premium{% endblock %}

{% block body %}
<div class="max-w-screen-lg mx-auto px-10 text-center h-screen">
    <a href="{{ path('app_subscription_checkout') }}" class="bg-green-700 rounded-md py-2 px-3 hover:bg-green-600 self-end w-fit transition-all duration-200 ease-in-out text-slate-50">
        {{ ux_icon('mdi:money', { 'class': 'h-5 w-5 text-slate-50 inline-block' }) }}
        Go Premium  
    </a>
</div>
{% endblock %}
